TODOs
=====

 - add aspectLibraries and their dependencies to the final artifact
    - I.)  use schema defined by aj mojo without version number of artifact (thus, add it as dependency to the project)
    - II.) define version number for each aspectLibrary and resolve it programmatically.
